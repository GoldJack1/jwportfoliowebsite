// Font Weight Constants
// These match the CSS custom properties defined in style.css
export const FONT_WEIGHTS = {
  thin: 100,
  extralight: 200,
  light: 300,
  regular: 400,
  medium: 500,
  semibold: 600,
  bold: 700,
  extrabold: 800,
  black: 900,
};

// For use in inline styles
export default FONT_WEIGHTS;
